## Codeflex

### Setup

#### Clone repository
    git clone

#### Run setup scripts (install npm, node, mysql)
    cd /setup/
    ./setup.sh

### Start 

    npm start   

    mvn clean
    mvn package
    cd target/ && java -jar web-0.0.1-SNAPSHOT.war
